
<%# il s'agit du code des card utiliser dans l'index.html.erb a voir ce qu'on en fait  %>



  <h1> Mes compagnons </h1>
  <div class="grid-container" pe>
    <% @pets.each do |list| %>
      <%= link_to pet_path(list.pet) do %>
        <div class="grid-item">
          <%= cl_image_tag list.pet.photo.key, class: "mini-avatar", alt: "Petit chat mignon"%>
          <h3><%= list.pet.name %></h3>
        </div>
      <% end %>
    <% end %>
  </div>
